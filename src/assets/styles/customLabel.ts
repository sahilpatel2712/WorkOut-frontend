import {StyleSheet} from 'react-native';

export const labelStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
  },
  labelText: {
    fontSize: 13,
    color: '#333',
    fontWeight:"500"
  },
});
