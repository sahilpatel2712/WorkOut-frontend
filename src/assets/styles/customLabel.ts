import {StyleSheet} from 'react-native';

export const labelStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
  },
  labelText: {
    fontSize: 13,
    color: '#686D76',
    fontWeight:"500"
  },
});
